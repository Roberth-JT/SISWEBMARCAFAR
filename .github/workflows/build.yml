name: SonarQube

on:
  push:
    branches:
      - master
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  build:
    name: Build and Analyze
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
  
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '7.0.x'

      - name: Install Java 17
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: 'zulu'

      - name: Install SonarCloud scanner
        run: dotnet tool install --global dotnet-sonarscanner

      - name: Build and analyze
        env:
          SONAR_TOKEN: ${{ c800488ef90cf90e9c4fd94634f2b9fc169534bc }}
        run: |
          dotnet-sonarscanner begin /k:"SISWEBMARCAFAR" /o:"Roberth-JT" /d:sonar.token="${{c800488ef90cf90e9c4fd94634f2b9fc169534bc }}"
          dotnet build
          dotnet-sonarscanner end /d:sonar.token="${{ c800488ef90cf90e9c4fd94634f2b9fc169534bc }}"
